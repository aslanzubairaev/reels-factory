anthropic>=0.39.0
openai>=1.50.0
google-genai>=1.0.0
requests>=2.31.0
Pillow>=10.0.0
jsonschema>=4.20.0

# Опционально — только для `/finish --with-subs` (субтитры через локальный Whisper).
# Тянет torch (~2 GB), поэтому отключено по умолчанию. Раскомментируй, если нужны субтитры:
# openai-whisper>=20231117
